# Claude / impeccable harness — local-only, never published
# (except project-level skills, which we DO want to commit)
.claude/*
!.claude/skills/
.claude/skills/impeccable/
.claude/skills/ui-ux-pro-max-skill/
.impeccable/

# Strategic docs — kept locally, not pushed
PRODUCT.md
DESIGN.md

# Paid course content — MUST stay out of the public repo (lives in Supabase).
# Drafts + generated seed SQL are local-only.
course-content/

# Deploy helpers — local-only
.deploy.py
.deploy.zip
.netlify/
.vercel/

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
*.swo
.vscode/
.idea/

# Node + Vite build
node_modules/
dist/
.vite-react-ssg-temp/
npm-debug.log
.env
.env.local

# Python bytecode (e.g. from skill scripts)
__pycache__/
*.pyc
legacy/
